# Implementation Plan: kgateway Controller Chart

**Branch**: `004-kgateway-chart` | **Date**: 2026-02-26 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `/specs/004-kgateway-chart/spec.md`

## Summary

Add a `kgateway-controller` Helm chart (see [spec.md](spec.md) for full requirements). The chart deploys the kgateway v2.2.1 control plane, follows existing controller chart patterns (common-lib + custom templates), and adds PDB + VPA support.

## Technical Context

**Language/Version**: Helm chart (YAML + Go templates)
**Primary Dependencies**: common-lib library chart (>=0.1.0 <1.0.0)
**Storage**: N/A
**Testing**: `helm lint`, `helm template`, chart-testing (`ct`)
**Target Platform**: Kubernetes 1.31+
**Project Type**: Helm chart (single chart within mono-repo)
**Constraints**: Gateway API CRDs v1.4.0 and kgateway-crds chart must be pre-installed

## Constitution Check

- ✅ Uses common-lib as dependency
- ✅ Follows canonical values shape (§5.2)
- ✅ Gates optional resources behind boolean feature flags
- ✅ 6 base labels + 2 base annotations on all resources
- ✅ CI test values provided
- ✅ README with all required sections (§9)
- ✅ Environment overlays for dev/staging/production
- ✅ CHANGELOG.md with initial version

## Project Structure

### Documentation (this feature)

```text
specs/004-kgateway-chart/
├── plan.md              # This file
├── spec.md              # Feature specification
├── research.md          # Technology decisions
├── data-model.md        # Kubernetes resource definitions
├── quickstart.md        # Installation walkthrough
├── contracts/           # YAML contracts for templates and CI values
│   └── chart-resources.md
└── tasks.md             # Task breakdown (generated by /speckit.tasks)
```

### Source Code (chart files)

```text
charts/kgateway-controller/
├── Chart.yaml
├── Chart.lock
├── CHANGELOG.md
├── README.md
├── values.yaml
├── charts/
│   └── common-lib-*.tgz
├── templates/
│   ├── _helpers.tpl
│   ├── deployment.yaml
│   ├── service.yaml
│   ├── serviceaccount.yaml
│   ├── clusterrole.yaml
│   ├── clusterrolebinding.yaml
│   ├── gateway-class.yaml
│   ├── hpa.yaml
│   ├── pdb.yaml
│   ├── vpa.yaml
│   └── NOTES.txt
└── ci/
    ├── test-values.yaml
    └── test-full-values.yaml

environments/
├── dev/kgateway-controller.values.yaml
├── staging/kgateway-controller.values.yaml
└── production/kgateway-controller.values.yaml

examples/
└── kgateway-controller-production.yaml
```

**Structure Decision**: Follows established controller chart pattern (traefik-controller, nginx-controller). Custom templates for Deployment, Service, and RBAC; common-lib delegation for ServiceAccount, HPA, labels, annotations, and naming.

> **Constitution §3.2 Note**: Templates "MUST primarily delegate to `common-lib` helpers." The Deployment, Service, ClusterRole, and ClusterRoleBinding use custom templates because the controller has unique needs (env-var config, multi-port, 12-apiGroup RBAC rules). This follows the same precedent set by `traefik-controller` and `nginx-controller`, which also use custom templates per §3.2's "unique needs" clause. Common-lib is used for labels, annotations, naming, ServiceAccount, and HPA.
