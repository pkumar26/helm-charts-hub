Thank you for installing {{ .Chart.Name }}!

Your release is named: {{ .Release.Name }}

To check the status of your deployment:

  kubectl get pods -l app.kubernetes.io/instance={{ .Release.Name }}

To check the service:

  kubectl get svc -l app.kubernetes.io/instance={{ .Release.Name }}

To port-forward to your application:

  kubectl port-forward svc/{{ include "common-lib.fullname" . }} 8080:{{ .Values.service.port }}

Then visit http://localhost:8080 in your browser.
{{- if .Values.ingress.enabled }}

Ingress is enabled. Your application should be accessible at the configured host(s).
{{- end }}
