# CI test values — ingress enabled
image:
  repository: nginx
  tag: "1.27-alpine"

ingress:
  enabled: true
  className: nginx
  hosts:
    - host: test.example.com
      paths:
        - path: /
          pathType: Prefix
