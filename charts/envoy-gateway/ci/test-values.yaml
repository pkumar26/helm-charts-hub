# Minimal CI test values for basic controller deployment
replicaCount: 1

image:
  pullPolicy: IfNotPresent

serviceAccount:
  create: true

rbac:
  create: true

gatewayClass:
  create: true

gateway:
  enabled: false

autoscaling:
  enabled: false
