apiVersion: v2
name: nginx-controller
description: NGINX Ingress Controller chart for helm-charts-hub — based on ingress-nginx for Kubernetes Ingress mode
type: application
version: 0.1.0
appVersion: "1.12.1"
dependencies:
  - name: common-lib
    version: ">=0.1.0 <1.0.0"
    repository: "file://../common-lib"
