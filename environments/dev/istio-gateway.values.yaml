# Dev cluster-specific values overlay for istio-gateway
# Use with: helm install -f values-dev.yaml -f environments/dev/istio-gateway.values.yaml

labels:
  cluster: dev-aks-01
  environment: dev
  region: eastus
  team: platform

annotations:
  deployment-target: "dev-aks-01.eastus.azure.com"
