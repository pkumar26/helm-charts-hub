# Traefik Controller — dev environment overlay
replicaCount: 1
service:
  type: ClusterIP
  ports:
    web: 8080
    websecure: 8443
resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 128Mi
dashboard:
  enabled: true
metrics:
  prometheus:
    enabled: false
