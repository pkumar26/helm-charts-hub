# Traefik Controller — staging environment overlay
replicaCount: 2
service:
  type: LoadBalancer
resources:
  requests:
    cpu: 200m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 256Mi
dashboard:
  enabled: false
metrics:
  prometheus:
    enabled: true
