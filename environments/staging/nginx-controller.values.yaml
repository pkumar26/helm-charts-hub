# NGINX Ingress Controller — staging environment overlay
replicaCount: 2
service:
  type: LoadBalancer
resources:
  requests:
    cpu: 200m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 256Mi
ingressController:
  config:
    use-forwarded-headers: "true"
    proxy-body-size: "50m"
